'use strict';
/*eslint-env jquery*/
const bookmark = (function() {
  //when add button is clicked, render the add view.
  $('#bookmark-condensed').on('submit', '.js-add-item', event => {
    event.preventDefault;
  });
})();
